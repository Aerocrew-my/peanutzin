import Link from"next/link";import{AdminShell,DataError}from"@/components/admin/shell";import{SocialStudioEditor}from"@/components/admin/social-studio";import{ArticleGenerator}from"@/components/admin/article-generator";import{isAiConfigured}from"@/lib/ai/config";import{createClient}from"@/lib/supabase/server";
export default async function SocialStudio({searchParams}:{searchParams:Promise<{draft?:string}>}){const{draft:id}=await searchParams,supabase=await createClient(),[articlesResult,draftsResult,draftResult,historyResult]=await Promise.all([supabase.from("articles").select("id,title,status,category").order("updated_at",{ascending:false}),supabase.from("social_drafts").select("id,title,status,updated_at,source_article_id").order("updated_at",{ascending:false}),id?supabase.from("social_drafts").select("*").eq("id",id).maybeSingle():Promise.resolve({data:null,error:null}),id?supabase.from("social_publications").select("*").eq("social_draft_id",id).order("created_at",{ascending:false}):Promise.resolve({data:[],error:null})]),error=[articlesResult,draftsResult,draftResult,historyResult].find(result=>result.error)?.error,articles=articlesResult.data??[],drafts=draftsResult.data??[],draft=draftResult.data,history=historyResult.data??[],configured=isAiConfigured();return <AdminShell section="Social Studio"><div className="admin-title"><h1>Social Studio</h1><Link className="button button-coral" href="/admin/social-studio">New draft</Link></div><p className="studio-intro">Idea → Generate Article → Human Review → Save to Articles → Generate Social → Approve → Publish. Nothing publishes without human approval.</p>{error?<DataError message={`Social Studio records could not be loaded (${error.message}).`}/>:<><ArticleGenerator configured={configured}/><SocialStudioEditor key={draft?.id??"new"} draft={draft??undefined} articles={articles} configured={configured} makeConfigured={Boolean(process.env.MAKE_SOCIAL_WEBHOOK_URL?.trim())} history={history}/><section className="studio-drafts"><h2>Saved social drafts</h2>{drafts.length?<div className="cms-list">{drafts.map(item=><Link href={`/admin/social-studio?draft=${item.id}`} key={item.id}><strong>{item.title||"Untitled draft"}</strong><span>{item.status}</span><small>Updated {new Date(item.updated_at).toLocaleDateString("en-MY")}</small></Link>)}</div>:<div className="empty-state"><h3>No social drafts yet.</h3><p>Select an article or start from a brief.</p></div>}</section></>}</AdminShell>}
