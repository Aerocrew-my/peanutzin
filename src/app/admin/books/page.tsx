import Link from"next/link";import{AdminShell,Notice}from"@/components/admin/shell";import{createClient}from"@/lib/supabase/server";import{formatMoney}from"@/lib/format";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams,{data}=await(await createClient()).from("books").select("*").order("updated_at",{ascending:false});return <AdminShell section="Books"><div className="admin-title"><h1>Books</h1><Link className="button button-coral" href="/admin/books/new">New book</Link></div><Notice searchParams={q}/><div className="cms-list">{(data??[]).map(x=><Link href={`/admin/books/${x.id}`} key={x.id}><strong>{x.title}</strong><span>{x.author} · {formatMoney(x.price_cents,x.currency)} · stock {x.stock_quantity??"—"}</span><small>{x.active?"Active":"Inactive"}{x.featured?" · featured":""}</small></Link>)}</div></AdminShell>}
