import{notFound}from"next/navigation";import{AdminShell,DataError}from"@/components/admin/shell";import{BookEditor,DeleteForm,deleteBook}from"@/components/admin/editors";import{createClient}from"@/lib/supabase/server";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const{data,error}=await(await createClient()).from("books").select("*").eq("id",id).maybeSingle();if(error)return <AdminShell section="Books"><h1>Edit book</h1><DataError message={`This book could not be loaded (${error.message}).`}/></AdminShell>;if(!data)notFound();return <AdminShell section="Books"><h1>Edit book</h1><BookEditor book={data}/><DeleteForm action={deleteBook} id={id} label="book"/></AdminShell>}
