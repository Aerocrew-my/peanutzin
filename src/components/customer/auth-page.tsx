import Link from "next/link";import{AuthForm}from"./auth-forms";import{Header,Footer,MobileNav}from"@/components/site";
export function CustomerAuthPage({mode}:{mode:"login"|"signup"|"forgot"}){const title=mode==="login"?"Welcome back":mode==="signup"?"Create your account":"Reset your password";return <><Header/><main className="customer-auth-page"><section><p className="eyebrow">PEANUTZIN account</p><h1>{title}</h1><p>{mode==="login"?"Sign in for your details and order history. Guest checkout is always available.":mode==="signup"?"Keep your orders together. Creating an account never grants staff access.":"We’ll email a secure link if an account exists."}</p><AuthForm mode={mode}/><Link className="auth-back" href="/books">← Continue browsing</Link></section></main><Footer/><MobileNav/></>}
