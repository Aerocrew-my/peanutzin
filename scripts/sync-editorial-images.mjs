import { createClient } from "@supabase/supabase-js";
import { readFile } from "node:fs/promises";
try{for(const line of (await readFile(new URL("../.env.local",import.meta.url),"utf8")).split(/\r?\n/)){const i=line.indexOf("=");if(i>0&&!line.startsWith("#")&&!process.env[line.slice(0,i)])process.env[line.slice(0,i)]=line.slice(i+1);}}catch{/* Deployment environments provide variables directly. */}
const url=process.env.NEXT_PUBLIC_SUPABASE_URL,anon=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,email=process.env.PEANUTZIN_TEST_ADMIN_EMAIL,password=process.env.PEANUTZIN_TEST_ADMIN_PASSWORD;
if(!url||!anon||!email||!password)throw new Error("Supabase URL, anon key, and active admin credentials are required for least-privilege media sync.");
if(!url.includes("kdyquipcyynugdyagecr"))throw new Error("Refusing to upload: the configured Supabase project is not the authorized PEANUTZIN project.");
const client=createClient(url,anon,{auth:{persistSession:false}}),signedIn=await client.auth.signInWithPassword({email,password});if(signedIn.error)throw new Error("Admin sign-in failed for media sync.");
const assets=[
 ["johor-book-bazaar.png","editorial/phase-7/johor-book-bazaar-editorial.png",3303701,"bazar-buku-antarabangsa-johor-2026","PEANUTZIN editorial artwork reading Johor Buku, 21–25 Ogos, with book spines and a paper ticket"],
 ["klibf-2027-partnership.png","editorial/phase-7/klibf-2027-exchange-editorial.png",3073438,"klibf-2027-plans-british-council-partnership","PEANUTZIN editorial artwork reading Kuala Lumpur ↔ UK and Books / Ideas / Exchange"],
 ["klibf-social-community.png","editorial/phase-7/klibf-social-community-editorial.png",3486170,"social-media-connects-writers-and-readers-at-klibf-2026","PEANUTZIN collage reading Books + People + Conversation with books, a phone and speech bubbles"],
 ["pena-manuscript-call.png","editorial/phase-7/pena-manuscript-call-editorial.png",3533438,"pena-malaysia-madani-opens-2026-manuscript-call","PEANUTZIN editorial artwork reading Manuskrip, PENA × Malaysia MADANI and 31 Dis 2026"],
 ["selangor-book-fair.png","editorial/phase-7/selangor-book-fair-editorial.png",2818757,"selangor-international-book-fair-2026","PEANUTZIN editorial artwork reading SIBF Selangor, 27 Nov — 6 Dis, with open books"],
];
const root=new URL("../supabase/assets/article-media/articles/",import.meta.url);
for(const[name,path,size,slug,alt]of assets){const body=await readFile(new URL(name,root));if(body.length!==size)throw new Error(`Local size mismatch for ${name}.`);const uploaded=await client.storage.from("article-media").upload(path,body,{contentType:"image/png",upsert:false});if(uploaded.error&&uploaded.error.message!=="The resource already exists")throw new Error(`Upload failed for ${path}: ${uploaded.error.message}`);const publicUrl=client.storage.from("article-media").getPublicUrl(path).data.publicUrl,response=await fetch(publicUrl,{cache:"no-store"});if(!response.ok||response.headers.get("content-type")?.split(";")[0]!=="image/png"||(await response.arrayBuffer()).byteLength!==size)throw new Error(`Public verification failed for ${path}.`);const updated=await client.from("articles").update({hero_image_path:path,hero_image_alt:alt}).eq("slug",slug);if(updated.error)throw new Error(`Article update failed for ${slug}: ${updated.error.message}`);console.log(`Verified ${path} image/png ${size} bytes and updated ${slug}`);}
